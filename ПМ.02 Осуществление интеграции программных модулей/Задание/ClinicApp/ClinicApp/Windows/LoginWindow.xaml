<Window x:Class="ClinicApp.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация" Height="441" Width="412" 
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">

    <Grid Margin="0,0,0,-6">
        <Grid Background="#2C3E50"/>

        <Border Background="White" CornerRadius="10" Margin="20,20,20,34" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Система учёта платных услуг" 
                           FontSize="18" FontWeight="Bold" 
                           HorizontalAlignment="Center" Margin="0,0,0,20"
                           Foreground="#2C3E50"/>

                <StackPanel Grid.Row="1" Margin="0,5">
                    <TextBlock Text="Логин" FontWeight="SemiBold"/>
                    <TextBox x:Name="LoginBox" Margin="0,2,0,10" Padding="8" FontSize="14"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Margin="0,5">
                    <TextBlock Text="Пароль" FontWeight="SemiBold"/>
                    <PasswordBox x:Name="PasswordBox" Margin="0,2,0,10" Padding="8" FontSize="14"/>
                </StackPanel>

                <Button Grid.Row="3" Content="Войти" Width="120" Height="40" 
                        Click="LoginBtn_Click" HorizontalAlignment="Center" 
                        Background="#3498DB" Foreground="White" 
                        FontWeight="Bold" FontSize="14"
                        Margin="0,10"/>

                <TextBlock Grid.Row="4" x:Name="ErrorText" 
                           Foreground="Red" HorizontalAlignment="Center" 
                           Margin="0,10" FontSize="13"/>
            </Grid>
        </Border>
    </Grid>
</Window>